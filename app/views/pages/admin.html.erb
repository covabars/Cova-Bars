<h2>&nbsp;</h2>
<h2>All Pages</h2>

<p><%= link_to 'Create new page', new_page_url %></p>

<ul>
<% @pages.each do |page| -%>
	<li>[ <%= link_to 'edit', edit_page_url(page) %>
	<!--| <%= link_to 'delete', page_url(page), :method => :delete, :confirm => 'Are you sure?' %> -->]
	: <span style="color: #FFFFFF; font-weight: bold;"><%= link_to page.title, page_url(page) %></span>
	</li>
<% end -%>
</ul>
